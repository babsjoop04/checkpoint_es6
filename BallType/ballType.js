
// donner par le site 
var Ball = function(ballType) {
    this.ballType = ballType || "regular"
};




//utilisation classe
class Ball {
    constructor(ballType){
      this.ballType=ballType || "regular"
    }
  }